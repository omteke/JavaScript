// The Document Object Model (DOM) in JavaScript is a programming interface 
// that represents a web page as a tree-like structure of objects. 
// This structure allows JavaScript to connect to the web page and 
// dynamically access, modify, add, or delete its content, structure, and styles in real-time, 
// without needing to reload the page. 

// we laern JavaScript and this cannot alone change webpage so we use DOM

// javascript uses DOM to connect webpages by making object of it

document.getElementById("title_id").id
document.getElementById("title_id").class
document.getElementById("title_id").className
document.getElementById("title_id").setAttribute
document.getElementById("title_id").getAttribute
